apiVersion: batch/v1
kind: Job
metadata:
  name: {{ template "kubeapps.mongodb-secret-bootstrap.fullname" . }}
  annotations:
    helm.sh/hook: pre-install
    helm.sh/hook-delete-policy: hook-succeeded
  labels:
    app: {{ template "kubeapps.mongodb-secret-bootstrap.fullname" . }}
    chart: {{ template "kubeapps.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  template:
    metadata:
      labels:
        app: {{ template "kubeapps.mongodb-secret-bootstrap.fullname" . }}
        release: {{ .Release.Name }}
    spec:
      containers:
      - name: kubectl
        image: {{ template "kubeapps.image" .Values.apprepository.jobsImage }}
        command:
        - kubectl
        - create
        - secret
        - generic
        - -n
        - {{ .Release.Namespace }}
        - {{ template "kubeapps.mongodb-secret.fullname" . }}
        - --from-literal
        - mongodb-root-password=`cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 32 | head -n 1`
        resources:
{{ toYaml .Values.apprepository.resources | indent 12 }}
      restartPolicy: OnFailure
      serviceAccountName: {{ template "kubeapps.mongodb-secret-bootstrap.fullname" . }}
    {{- with .Values.apprepository.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.apprepository.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.apprepository.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
    {{- end }}
